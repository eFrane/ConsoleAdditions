language: php

cache:
  directories:
    - vendor/

php:
  - 5.6
  - '7.0'
  - '7.1'
  - hhvm

allowed_failures:
  - hhvm

before_script:
  - composer install --prefer-source --no-interaction --dev
  - composer dump-autoload

script: vendor/bin/phpunit
